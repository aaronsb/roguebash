#!/bin/bash
# attack — TODO: implement (see tools/README.md for the contract)
set -euo pipefail

if [ "${1:-}" = "--schema" ]; then
  cat <<'JSON'
{
  "name": "attack",
  "description": "TODO: describe what this tool does",
  "parameters": { "type": "object", "properties": {}, "required": [] }
}
JSON
  exit 0
fi

echo "attack: not yet implemented" >&2
exit 2
