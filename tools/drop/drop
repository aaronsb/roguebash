#!/bin/bash
# drop — TODO: implement (see tools/README.md for the contract)
set -euo pipefail

if [ "${1:-}" = "--schema" ]; then
  cat <<'JSON'
{
  "name": "drop",
  "description": "TODO: describe what this tool does",
  "parameters": { "type": "object", "properties": {}, "required": [] }
}
JSON
  exit 0
fi

echo "drop: not yet implemented" >&2
exit 2
